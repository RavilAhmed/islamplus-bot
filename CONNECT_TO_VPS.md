# 🚀 Подключение к VPS и установка бота

## Ваши данные:
- **IP-адрес:** `194.67.127.172`
- **ОС:** Ubuntu 24.04 LTS
- **Имя сервера:** Ahmedsnake

---

## ШАГ 1: Подключитесь к VPS

**В Терминале на Mac выполните:**

```bash
ssh root@194.67.127.172
```

**Или если у вас другой пользователь (не root):**

```bash
ssh ваш_пользователь@194.67.127.172
```

**Что должно получиться:**

**Вариант 1 (первое подключение):**
```
The authenticity of host '194.67.127.172 (194.67.127.172)' can't be established.
ECDSA key fingerprint is SHA256:...
Are you sure you want to continue connecting (yes/no/[fingerprint])? 
```

**Напишите:** `yes` и нажмите Enter

**Вариант 2 (если уже подключались):**
```
root@194.67.127.172's password:
```

**Введите пароль** для пользователя root (пароль будет скрыт - это нормально)

**После успешного подключения вы увидите:**

```
Welcome to Ubuntu 24.04 LTS
...
root@Ahmedsnake:~#
```

**✅ Если видите `root@Ahmedsnake:~#` - вы подключены к VPS!**

---

## ШАГ 2: Обновите систему (рекомендуется)

**На VPS выполните:**

```bash
apt update
apt upgrade -y
```

**Это может занять 2-5 минут. Подождите, пока закончится.**

---

## ШАГ 3: Установите необходимые пакеты

**На VPS выполните:**

```bash
apt install -y python3 python3-pip python3-venv git postgresql postgresql-contrib
```

**Подождите, пока установятся пакеты (2-5 минут).**

---

## ШАГ 4: Клонируйте репозиторий

**На VPS выполните:**

```bash
cd /opt
git clone https://github.com/RavilAhmed/islamplus-bot.git
cd /opt/islamplus-bot
```

**Что должно получиться:**
```
Cloning into 'islamplus-bot'...
remote: Enumerating objects: 55, done.
remote: Counting objects: 100% (55/55), done.
remote: Compressing objects: 100% (43/43), done.
remote: Total 55 (delta 1), reused 55 (delta 1), pack-reused 0
Receiving objects: 100% (55/55), done.
Resolving deltas: 100% (1/1), done.
```

**✅ Если видите "done" - репозиторий клонирован!**

---

## ШАГ 5: Запустите установку

**На VPS выполните:**

```bash
chmod +x install.sh
./install.sh
```

**Скрипт установки спросит:**

**1. Git URL (если спросит):**
- Введите: `https://github.com/RavilAhmed/islamplus-bot.git`
- Или просто нажмите Enter, если URL уже правильный

**2. Про PostgreSQL:**
- Если спросит "У вас уже настроена PostgreSQL? (y/n)"
- Если PostgreSQL уже установлен (так как вы делали `apt install postgresql`) - ответьте `y`
- Если нет - ответьте `n`, и скрипт установит его

**Подождите 5-10 минут, пока скрипт все установит.**

---

## ШАГ 6: Настройте .env файл

**После установки на VPS выполните:**

```bash
nano /opt/islamplus-bot/.env
```

**Отредактируйте файл:**

1. **Найдите строку:** `BOT_TOKEN=`
2. **Замените на:** `BOT_TOKEN=ваш_токен_от_BotFather`
   - Получить токен: найдите @BotFather в Telegram → /newbot → следуйте инструкциям
3. **Найдите строку:** `ADMIN_IDS=`
4. **Замените на:** `ADMIN_IDS=ваш_telegram_id`
   - Узнать ID: найдите @userinfobot в Telegram → /start

**Как сохранить в nano:**
- Нажмите `Ctrl + X` для выхода
- Нажмите `Y` для сохранения
- Нажмите `Enter` для подтверждения

---

## ШАГ 7: Инициализируйте базу данных

**На VPS выполните:**

```bash
cd /opt/islamplus-bot
source venv/bin/activate
python3 init_db.py
```

**Что должно получиться:**
```
INFO - Инициализация базы данных...
INFO - База данных успешно инициализирована!
```

**✅ Если видите "успешно инициализирована" - БД готова!**

---

## ШАГ 8: Перезапустите бота

**На VPS выполните:**

```bash
systemctl restart islamplus-bot
```

**Проверьте статус:**

```bash
systemctl status islamplus-bot
```

**Что должно получиться:**
```
● islamplus-bot.service - IslamPlus.Practice Telegram Bot
   Loaded: loaded (/etc/systemd/system/islamplus-bot.service; enabled; vendor preset: enabled)
   Active: active (running) since ...
```

**✅ Если видите "active (running)" - бот запущен!**

---

## ШАГ 9: Проверьте логи

**На VPS выполните:**

```bash
journalctl -u islamplus-bot -n 50
```

**Если видите ошибки - напишите мне, что именно.**

---

## ✅ ГОТОВО!

Теперь откройте Telegram:
1. Найдите вашего бота по username
2. Отправьте команду `/start`
3. Бот должен ответить!

---

## ❓ Если что-то не работает

**Бот не запускается:**
```bash
journalctl -u islamplus-bot -n 100
```

**Проверьте .env файл:**
```bash
cat /opt/islamplus-bot/.env | grep BOT_TOKEN
```

**Перезапустите бота:**
```bash
systemctl restart islamplus-bot
```

---

## 🔄 Обновление бота в будущем

**Когда вы измените код на Mac и запушите в GitHub:**

**На VPS выполните:**

```bash
cd /opt/islamplus-bot
./deploy.sh
```

**Или просто:**
```bash
cd /opt/islamplus-bot
git pull
systemctl restart islamplus-bot
```

---

**Начинайте с ШАГА 1! Пишите, если что-то непонятно или возникнут ошибки.** 🚀