# 🐙 Подробная инструкция: Загрузка кода в GitHub

Пошаговая инструкция для пользователя @RavilAhmed

## Шаг 1: Подготовка на компьютере

### 1.1 Откройте терминал в папке проекта

**На Mac:**
```bash
cd "/Users/ravilahmed/Desktop/Бот Ислам и Практика"
```

**На Windows:**
```powershell
cd "C:\Users\ВашеИмя\Desktop\Бот Ислам и Практика"
```

### 1.2 Проверьте, установлен ли Git

```bash
git --version
```

Если Git не установлен:
- **Mac:** `brew install git` или скачайте с https://git-scm.com/download/mac
- **Windows:** Скачайте с https://git-scm.com/download/win

### 1.3 Настройте Git (если еще не настроен)

```bash
# Замените на ваши данные
git config --global user.name "RavilAhmed"
git config --global user.email "your-email@example.com"
```

### 1.4 Инициализируйте Git репозиторий

```bash
# В папке проекта
git init

# Проверяем статус
git status
```

Вы должны увидеть список всех файлов проекта.

### 1.5 Добавьте все файлы

```bash
# Добавляем все файлы
git add .

# Проверяем, что добавлено
git status
```

### 1.6 Создайте первый коммит

```bash
git commit -m "Initial commit: IslamPlus.Practice Bot"
```

---

## Шаг 2: Создание репозитория на GitHub

### 2.1 Откройте GitHub

1. Перейдите на https://github.com/RavilAhmed
2. Убедитесь, что вы вошли в аккаунт (кнопка "Sign in" в правом верхнем углу)

### 2.2 Создайте новый репозиторий

**Вариант 1: Через кнопку**
1. Нажмите зеленую кнопку **"New"** (или знак `+` в правом верхнем углу)
2. Выберите **"New repository"**

**Вариант 2: Прямая ссылка**
- Перейдите на https://github.com/new

### 2.3 Заполните форму

```
Repository name:  islamplus-bot
                 (или любое другое название, например: islam-practice-bot)

Description:      Telegram bot for Islamic practice and courses
                 (опционально, можно оставить пустым)

Visibility:       ○ Private  ← выберите для приватного репозитория
                  ○ Public     ← или выберите для публичного

❌ НЕ устанавливайте галочки:
   ☐ Add a README file
   ☐ Add .gitignore
   ☐ Choose a license
   
   (У нас уже есть все эти файлы!)
```

### 2.4 Создайте репозиторий

Нажмите зеленую кнопку **"Create repository"**

---

## Шаг 3: Подключение локального репозитория к GitHub

### 3.1 GitHub покажет вам инструкции

После создания репозитория вы увидите страницу с заголовком:
**"Quick setup — if you've done this kind of thing before"**

### 3.2 Выберите HTTPS (рекомендуется)

Используйте команды из раздела **"...or push an existing repository from the command line"**

### 3.3 Выполните команды в терминале

**ВАЖНО:** Замените `RavilAhmed` на ваш реальный username, если он отличается!

```bash
# Подключаем удаленный репозиторий
git remote add origin https://github.com/RavilAhmed/islamplus-bot.git

# Переименуем ветку в main (если нужно)
git branch -M main

# Отправляем код на GitHub
git push -u origin main
```

### 3.4 Авторизация в GitHub

При выполнении `git push` GitHub попросит авторизоваться:

**Вариант 1: Через браузер (рекомендуется)**
- Откроется окно браузера для авторизации
- Нажмите "Authorize" или "Sign in"

**Вариант 2: Через Personal Access Token**
1. Перейдите: https://github.com/settings/tokens
2. Нажмите **"Generate new token"** → **"Generate new token (classic)"**
3. Заполните:
   - **Note:** `Git push token`
   - **Expiration:** выберите срок действия
   - **Select scopes:** установите галочку `repo` (полный доступ)
4. Нажмите **"Generate token"**
5. **Скопируйте токен** (он показывается только один раз!)
6. При `git push` используйте токен вместо пароля:
   - Username: ваш GitHub username
   - Password: вставьте токен

### 3.5 Проверка

После успешного `git push` вы должны увидеть:
```
Enumerating objects: XX, done.
Counting objects: 100% (XX/XX), done.
...
To https://github.com/RavilAhmed/islamplus-bot.git
 * [new branch]      main -> main
Branch 'main' set up to track remote branch 'main' from 'origin'.
```

### 3.6 Проверьте на GitHub

Обновите страницу https://github.com/RavilAhmed/islamplus-bot

**Вы должны увидеть:**
- ✅ Все ваши файлы (`src/`, `requirements.txt`, `README.md` и т.д.)
- ✅ Коммит с сообщением "Initial commit: IslamPlus.Practice Bot"

---

## ✅ Готово!

Код успешно загружен в GitHub! 🎉

Теперь можете переходить к следующему шагу - установке на VPS.

---

## 🔄 Обновление кода в будущем

Когда вы измените код на компьютере:

```bash
# 1. Перейдите в папку проекта
cd "/Users/ravilahmed/Desktop/Бот Ислам и Практика"

# 2. Проверьте, что изменилось
git status

# 3. Добавьте изменения
git add .

# 4. Создайте коммит
git commit -m "Описание ваших изменений"

# 5. Отправьте на GitHub
git push
```

---

## ❓ Решение проблем

### Ошибка: "remote origin already exists"

```bash
# Удалите старый remote
git remote remove origin

# Добавьте заново
git remote add origin https://github.com/RavilAhmed/islamplus-bot.git
```

### Ошибка: "Authentication failed"

- Проверьте, правильно ли указан username
- Используйте Personal Access Token вместо пароля
- Убедитесь, что токен имеет права `repo`

### Ошибка: "Permission denied"

- Убедитесь, что вы владелец репозитория
- Проверьте права доступа в настройках репозитория

---

**Следующий шаг:** Установка на VPS (см. INSTALL_STEP_BY_STEP.md)