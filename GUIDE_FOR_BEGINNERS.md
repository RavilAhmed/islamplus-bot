# 📖 Руководство для начинающих: Где что делать

Простое объяснение, на каком устройстве выполнять каждый шаг.

---

## 🖥️ Два места работы

Есть **2 места**, где мы будем работать:

1. **Ваш Mac (компьютер)** - здесь сейчас лежит проект
2. **VPS сервер** (удаленный сервер) - здесь будет работать бот

---

## 📍 Шаг 1: Подготовка на Mac

### Где выполнять: **На вашем Mac (компьютере)**

**Что делаем:** Загружаем код в GitHub

**Почему:** Чтобы код был доступен в интернете, и мы могли его скачать на VPS

### Шаги 1.1 - 1.6: ✅ НА МАКЕ

1. Откройте **Терминал** на Mac
2. Перейдите в папку проекта:
   ```bash
   cd "/Users/ravilahmed/Desktop/Бот Ислам и Практика"
   ```
3. Инициализируйте Git:
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   ```

### Шаг 1.7: ✅ В БРАУЗЕРЕ НА МАКЕ

1. Откройте браузер (Safari, Chrome и т.д.)
2. Перейдите на https://github.com/RavilAhmed
3. Создайте новый репозиторий (кнопка "New")

### Шаг 1.8: ✅ СНОВА В ТЕРМИНАЛЕ НА МАКЕ

1. Вернитесь в Терминал
2. Подключите репозиторий и отправьте код:
   ```bash
   git remote add origin https://github.com/RavilAhmed/islamplus-bot.git
   git push -u origin main
   ```

**Результат шага 1:** Код теперь в GitHub! ✅

---

## 📍 Шаг 2: Подключение к VPS

### Где выполнять: **В терминале на Mac, но подключаемся к VPS**

**Что делаем:** Подключаемся к удаленному серверу (VPS)

### Шаг 2.1: ✅ В БРАУЗЕРЕ НА МАКЕ

1. Откройте https://cloud.reg.ru/panel/
2. Найдите IP-адрес вашего VPS
3. Скопируйте IP-адрес (например: `123.45.67.89`)

### Шаг 2.2: ✅ В ТЕРМИНАЛЕ НА МАКЕ (подключение к VPS)

```bash
# Замените IP_ADDRESS на ваш IP из панели Reg.ru
ssh root@IP_ADDRESS

# Например:
ssh root@123.45.67.89
```

**После этой команды:**
- Вы подключились к VPS
- Терминал теперь работает **на удаленном сервере**
- Приглашение командной строки изменится (например: `root@vps:~#`)

**Как понять, что вы на VPS:**
- В начале строки будет IP или имя сервера
- Вы больше НЕ на своем Mac

---

## 📍 Шаг 3-6: Установка на VPS

### Где выполнять: **На VPS (через SSH подключение)**

**⚠️ ВАЖНО:** Все дальнейшие команды выполняются **НА VPS**, не на Mac!

### Как понять, что вы на VPS:
- Приглашение командной строки показывает `root@vps` или IP-адрес
- Вы подключены через `ssh`

### Шаги 3-6: ✅ НА VPS (через SSH)

Все команды выполняйте в том же терминале, где вы подключились к VPS.

**Пример:**
```bash
# Вы на VPS (видно по приглашению root@vps:~#)
cd /opt
sudo git clone https://github.com/RavilAhmed/islamplus-bot.git
cd /opt/islamplus-bot
sudo ./install.sh
```

---

## 🔄 Обновление бота (после установки)

### В будущем, когда вы измените код:

**1. На Mac (компьютере):**
```bash
cd "/Users/ravilahmed/Desktop/Бот Ислам и Практика"

# Редактируете файлы в VS Code или другом редакторе

# Сохраняете изменения в Git
git add .
git commit -m "Описание изменений"
git push
```

**2. На VPS (подключаетесь и обновляете):**
```bash
# Подключаетесь к VPS
ssh root@ВАШ_IP

# Обновляете бота
cd /opt/islamplus-bot
sudo ./deploy.sh

# Выходите с VPS
exit
```

---

## 📝 Краткая памятка

| Действие | Где выполнять |
|----------|---------------|
| Редактировать код | Mac (VS Code, PyCharm и т.д.) |
| Загрузить в GitHub | Mac (Терминал) |
| Подключиться к VPS | Mac (Терминал: `ssh root@IP`) |
| Установить бота | VPS (после подключения через SSH) |
| Обновить бота | VPS (после подключения через SSH) |

---

## 🎯 Схема работы

```
┌─────────────────┐         ┌──────────┐         ┌──────────┐
│   Ваш Mac       │         │  GitHub  │         │   VPS    │
│  (компьютер)    │         │          │         │ (сервер) │
└─────────────────┘         └──────────┘         └──────────┘
       │                           │                    │
       │ 1. git push               │                    │
       │──────────────────────────>│                    │
       │                           │                    │
       │                           │ 2. git clone       │
       │                           │───────────────────>│
       │                           │                    │
       │ 3. ssh root@IP            │                    │
       │───────────────────────────────────────────────>│
       │                           │                    │
       │                           │ 4. Управление      │
       │                           │    через SSH       │
       │<───────────────────────────────────────────────│
```

---

## ✅ Проверочный список

**На Mac вы:**
- [ ] Открыли Терминал
- [ ] Перешли в папку проекта
- [ ] Выполнили `git init`, `git add .`, `git commit`
- [ ] Создали репозиторий на GitHub
- [ ] Выполнили `git push`

**На VPS вы:**
- [ ] Узнали IP-адрес из панели Reg.ru
- [ ] Подключились через `ssh root@IP`
- [ ] Убедились, что находитесь на VPS (видно по приглашению)
- [ ] Выполняете команды установки

---

## ❓ Как понять, где я сейчас?

**В терминале посмотрите на приглашение:**

**На Mac:**
```bash
ravilahmed@MacBook-Pro ~ %
# или
ravilahmed:Бот Ислам и Практика ravilahmed$
```

**На VPS:**
```bash
root@vps:~#
# или
root@123.45.67.89:~#
```

**Если видите `root@` или IP-адрес - вы на VPS!** ✅

**Если видите имя вашего Mac - вы на Mac!** 🖥️

---

## 🆘 Выход с VPS

Когда закончите работу на VPS:
```bash
exit
```

Или нажмите `Ctrl + D`

После этого вы вернетесь в терминал на Mac.

---

**Теперь понятно, где что делать?** 😊

Если есть вопросы - спрашивайте!