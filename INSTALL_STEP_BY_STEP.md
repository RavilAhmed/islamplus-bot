# 📚 Подробная пошаговая инструкция по установке

Пошаговое руководство от загрузки кода в GitHub до запуска бота на VPS Reg.ru.

---

## 📋 Содержание

1. [Подготовка: загрузка кода в GitHub](#1-подготовка-загрузка-кода-в-github)
2. [Подключение к VPS Reg.ru](#2-подключение-к-vps-regru)
3. [Установка бота на VPS](#3-установка-бота-на-vps)
4. [Настройка бота](#4-настройка-бота)
5. [Проверка работы](#5-проверка-работы)
6. [Обновление бота](#6-обновление-бота)

---

## 1. Подготовка: Загрузка кода в GitHub

> **⚠️ ВАЖНО:** Шаги 1.1-1.6 выполняются на **ВАШЕМ КОМПЬЮТЕРЕ (Mac)**, где сейчас находится проект!
> 
> VPS пока не нужен - мы сначала загрузим код в GitHub с компьютера, а потом установим на VPS.

### Шаг 1.1: Инициализация Git репозитория (на вашем Mac)

**Где:** На вашем Mac, в терминале

**Что делать:**
1. Откройте **Терминал** на Mac (через Spotlight: `Cmd + Space`, введите "Терминал")
2. Перейдите в папку проекта:
   ```bash
   cd "/Users/ravilahmed/Desktop/Бот Ислам и Практика"
   ```
3. Выполните команды для подготовки к загрузке в GitHub:
   ```bash
   # Инициализируем Git (если еще не инициализирован)
   git init

   # Добавляем все файлы
   git add .

   # Создаем первый коммит
   git commit -m "Initial commit: IslamPlus.Practice Bot"
   ```

**Результат:** Код подготовлен для отправки в GitHub (но еще не отправлен)

### Шаг 1.2: Создание репозитория на GitHub

1. **Откройте** https://github.com/RavilAhmed
2. **Нажмите** кнопку **"New"** (зеленая кнопка) или перейдите по ссылке https://github.com/new
3. **Заполните форму:**
   - **Repository name:** `islamplus-bot` (или любое другое название)
   - **Description:** `Telegram bot for Islamic practice and courses`
   - **Visibility:** выберите **Private** (приватный) или **Public** (публичный)
   - **НЕ устанавливайте галочки** на "Add README", "Add .gitignore" и т.д. (у нас уже есть файлы)
4. **Нажмите** кнопку **"Create repository"**

### Шаг 1.3: Подключение локального репозитория к GitHub

После создания репозитория GitHub покажет вам команды. Выполните их в терминале:

```bash
# Замените YOUR_USERNAME на ваш GitHub username (RavilAhmed)
git remote add origin https://github.com/RavilAhmed/islamplus-bot.git

# Или если используете SSH (рекомендуется для VPS):
# git remote add origin git@github.com:RavilAhmed/islamplus-bot.git

# Переименуем ветку в main (если нужно)
git branch -M main

# Отправляем код на GitHub
git push -u origin main
```

Если GitHub запросит авторизацию:
- Используйте **Personal Access Token** вместо пароля
- Получить токен: GitHub → Settings → Developer settings → Personal access tokens → Generate new token
- Дайте токену права: `repo` (полный доступ к репозиториям)

### ✅ Проверка

Откройте https://github.com/RavilAhmed/islamplus-bot - вы должны увидеть все ваши файлы!

---

## 2. Подключение к VPS Reg.ru

### Шаг 2.1: Получение данных для доступа к VPS

1. **Откройте панель управления:** https://cloud.reg.ru/panel/
2. **Войдите** в свой аккаунт
3. **Найдите** ваш VPS сервер в списке
4. **Нажмите** на сервер, чтобы открыть детали
5. **Найдите информацию:**
   - **IP-адрес** сервера (например: `123.45.67.89`)
   - **Логин** (обычно `root` или другой пользователь)
   - **Пароль** (если используете пароль) или настройте SSH ключ

### Шаг 2.2: Подключение через SSH (Mac/Linux)

Откройте терминал на вашем компьютере:

```bash
# Замените USERNAME на ваш логин (обычно root) и IP_ADDRESS на IP вашего VPS
ssh USERNAME@IP_ADDRESS

# Например:
ssh root@123.45.67.89
```

При первом подключении появится предупреждение - нажмите `yes`.

Введите пароль (если используете пароль).

### Шаг 2.3: Подключение через SSH (Windows)

**Вариант 1: PowerShell (Windows 10+)**
```powershell
ssh root@IP_ADDRESS
```

**Вариант 2: PuTTY**
1. Скачайте PuTTY: https://www.putty.org/
2. Укажите IP-адрес и порт (22)
3. Нажмите "Open"
4. Введите логин и пароль

### ✅ Проверка

Вы должны увидеть приглашение командной строки сервера:
```
root@vps:~#
```

---

## 3. Установка бота на VPS

### Шаг 3.1: Обновление системы

```bash
# Обновляем список пакетов
sudo apt update

# Обновляем систему (если нужно)
sudo apt upgrade -y
```

### Шаг 3.2: Клонирование репозитория

```bash
# Переходим в папку /opt
cd /opt

# Клонируем репозиторий
sudo git clone https://github.com/RavilAhmed/islamplus-bot.git

# Переходим в папку проекта
cd /opt/islamplus-bot

# Проверяем, что файлы на месте
ls -la
```

**Вы должны увидеть:**
- `src/` - папка с кодом
- `deploy.sh` - скрипт обновления
- `install.sh` - скрипт установки
- `requirements.txt` - зависимости
- и другие файлы

### Шаг 3.3: Запуск автоматической установки

```bash
# Даем права на выполнение скрипту
sudo chmod +x install.sh

# Запускаем установку
sudo ./install.sh
```

**Скрипт автоматически:**
1. Установит Python 3 и необходимые пакеты
2. Создаст виртуальное окружение
3. Установит зависимости Python
4. Настроит PostgreSQL (если нужно)
5. Создаст systemd сервис
6. Запустит бота

### Шаг 3.4: Если установка запросит данные

**Если спросит про Git репозиторий:**
- Введите: `https://github.com/RavilAhmed/islamplus-bot.git`

**Если спросит про PostgreSQL:**
- Если у вас уже есть PostgreSQL - ответьте `y`
- Если нет - ответьте `n`, и скрипт установит его автоматически

**Внимание!** Скрипт может запросить подтверждение установки пакетов - введите `y` (yes)

### ✅ Проверка установки

```bash
# Проверяем статус бота
sudo systemctl status islamplus-bot
```

Вы должны увидеть `active (running)` или `failed` (если нужно настроить .env)

---

## 4. Настройка бота

### Шаг 4.1: Получение BOT_TOKEN от BotFather

1. **Откройте Telegram**
2. **Найдите бота** @BotFather
3. **Отправьте команду:** `/newbot`
4. **Следуйте инструкциям:**
   - Придумайте имя бота (например: `IslamPlus Practice`)
   - Придумайте username бота (например: `islamplus_practice_bot` - должен заканчиваться на `bot`)
5. **BotFather отправит вам токен**, например:
   ```
   1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
   ```
6. **Скопируйте токен** - он понадобится!

### Шаг 4.2: Получение вашего Telegram ID

1. **Откройте Telegram**
2. **Найдите бота** @userinfobot
3. **Отправьте команду:** `/start`
4. **Бот отправит ваш ID**, например: `123456789`
5. **Скопируйте ID** - он понадобится!

### Шаг 4.3: Настройка файла .env

```bash
# Открываем файл .env для редактирования
sudo nano /opt/islamplus-bot/.env
```

**Отредактируйте файл, указав:**

```env
# Токен бота от @BotFather
BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz

# URL базы данных (обычно не нужно менять, если использовали установку)
DATABASE_URL=postgresql+asyncpg://islamplus:islamplus_password@localhost:5432/islamplus

# Redis (опционально, можно оставить как есть)
REDIS_URL=redis://localhost:6379/0

# Ваш Telegram ID (от @userinfobot)
ADMIN_IDS=123456789

# Остальные настройки можно оставить по умолчанию
DAILY_FOCUS_LIMIT=5
POINTS_MULTIPLIER_FOCUS=2
COMBO_BONUS_POINTS=50
MAX_STREAK_MULTIPLIER=3.0
MORNING_REMINDER=06:00
EVENING_REMINDER=17:00
DAILY_REMINDER=20:00
```

**Как сохранить в nano:**
- Нажмите `Ctrl + X` для выхода
- Нажмите `Y` для подтверждения сохранения
- Нажмите `Enter` для подтверждения имени файла

### Шаг 4.4: Изменение пароля базы данных (рекомендуется)

**Если скрипт установки создал базу данных с паролем по умолчанию:**

1. **Создайте новый надежный пароль** (например: `MySecureP@ssw0rd123`)

2. **Измените пароль в PostgreSQL:**
```bash
sudo -u postgres psql
```

В psql выполните:
```sql
ALTER USER islamplus WITH PASSWORD 'MySecureP@ssw0rd123';
\q
```

3. **Обновите .env файл:**
```bash
sudo nano /opt/islamplus-bot/.env
```

Измените строку:
```env
DATABASE_URL=postgresql+asyncpg://islamplus:MySecureP@ssw0rd123@localhost:5432/islamplus
```

### Шаг 4.5: Инициализация базы данных

```bash
cd /opt/islamplus-bot

# Активируем виртуальное окружение
source venv/bin/activate

# Инициализируем базу данных
python3 init_db.py
```

**Вы должны увидеть:**
```
INFO - Инициализация базы данных...
INFO - База данных успешно инициализирована!
```

### Шаг 4.6: Перезапуск бота

```bash
# Перезапускаем бота с новыми настройками
sudo systemctl restart islamplus-bot

# Проверяем статус
sudo systemctl status islamplus-bot
```

**Вы должны увидеть:**
```
● islamplus-bot.service - IslamPlus.Practice Telegram Bot
   Loaded: loaded (/etc/systemd/system/islamplus-bot.service; enabled; vendor preset: enabled)
   Active: active (running) since ...
```

### ✅ Проверка логов

```bash
# Смотрим последние логи
sudo journalctl -u islamplus-bot -n 50

# Или смотреть логи в реальном времени
sudo journalctl -u islamplus-bot -f
```

**Если видите ошибки:**
- Проверьте, правильно ли указан `BOT_TOKEN` в `.env`
- Убедитесь, что база данных работает: `sudo systemctl status postgresql`
- Проверьте права доступа: `sudo chown -R www-data:www-data /opt/islamplus-bot`

---

## 5. Проверка работы

### Шаг 5.1: Тест в Telegram

1. **Откройте Telegram**
2. **Найдите вашего бота** по username (например: `@islamplus_practice_bot`)
3. **Нажмите** "Start" или отправьте команду `/start`

**Должен прийти ответ:**
```
Ассаляму алейкум, [имя]! 👋

Добро пожаловать в **IslamPlus.Practice** — бот для системного развития мусульманина.
...
```

### Шаг 5.2: Проверка команд

Проверьте, что работают все команды:
- `/start` - приветствие
- `/menu` - главное меню
- `/progress` - прогресс (может быть пустым, это нормально)

### ✅ Если что-то не работает

**Бот не отвечает:**
```bash
# Проверьте логи
sudo journalctl -u islamplus-bot -n 100

# Проверьте статус
sudo systemctl status islamplus-bot

# Перезапустите
sudo systemctl restart islamplus-bot
```

**Ошибки в логах:**
- `BOT_TOKEN не установлен` - проверьте `.env` файл
- `Ошибка подключения к БД` - проверьте `DATABASE_URL` в `.env`
- `Permission denied` - проверьте права: `sudo chown -R www-data:www-data /opt/islamplus-bot`

---

## 6. Обновление бота

### Простой способ (3 команды)

Когда вы изменили код на компьютере и запушили в GitHub:

**На сервере:**
```bash
cd /opt/islamplus-bot
sudo -u www-data git pull
sudo systemctl restart islamplus-bot
```

### Автоматический способ (1 команда)

**На сервере:**
```bash
cd /opt/islamplus-bot
sudo ./deploy.sh
```

Этот скрипт автоматически:
1. ✅ Обновит код из Git
2. ✅ Установит новые зависимости (если есть)
3. ✅ Инициализирует БД (если нужно)
4. ✅ Перезапустит бота

---

## 📝 Типичный workflow (рабочий процесс)

### Ежедневная работа:

**1. На компьютере (разработка):**
```bash
cd "/Users/ravilahmed/Desktop/Бот Ислам и Практика"

# Редактируете файлы в редакторе (VS Code, PyCharm и т.д.)

# Сохраняете изменения в Git
git add .
git commit -m "Добавлена новая функция"
git push
```

**2. На VPS (обновление):**
```bash
# Подключаетесь к серверу
ssh root@YOUR_IP_ADDRESS

# Обновляете бота (1 команда!)
cd /opt/islamplus-bot && sudo ./deploy.sh

# Или просто (2 команды):
cd /opt/islamplus-bot
sudo -u www-data git pull && sudo systemctl restart islamplus-bot
```

**Готово!** Бот обновлен! 🎉

---

## 🔧 Полезные команды

### Управление ботом:

```bash
# Статус бота
sudo systemctl status islamplus-bot

# Запуск
sudo systemctl start islamplus-bot

# Остановка
sudo systemctl stop islamplus-bot

# Перезапуск
sudo systemctl restart islamplus-bot

# Просмотр логов (последние 50 строк)
sudo journalctl -u islamplus-bot -n 50

# Просмотр логов в реальном времени
sudo journalctl -u islamplus-bot -f
```

### Работа с Git:

```bash
cd /opt/islamplus-bot

# Проверить статус
git status

# Посмотреть последние коммиты
git log --oneline -10

# Откатить изменения (если что-то пошло не так)
sudo -u www-data git reset --hard origin/main
```

### Работа с базой данных:

```bash
# Подключиться к PostgreSQL
sudo -u postgres psql -d islamplus

# В psql посмотреть таблицы:
\dt

# Выход из psql:
\q
```

---

## ❓ Решение проблем

### Проблема: "Permission denied"

```bash
sudo chown -R www-data:www-data /opt/islamplus-bot
sudo chmod +x /opt/islamplus-bot/deploy.sh
sudo chmod +x /opt/islamplus-bot/install.sh
```

### Проблема: "Бот не запускается"

```bash
# Проверьте логи
sudo journalctl -u islamplus-bot -n 100

# Проверьте .env файл
sudo nano /opt/islamplus-bot/.env

# Проверьте права
sudo chown -R www-data:www-data /opt/islamplus-bot
```

### Проблема: "Git pull не работает"

```bash
# Проверьте права на Git
sudo -u www-data git pull

# Или установите права на папку .git
sudo chown -R www-data:www-data /opt/islamplus-bot/.git
```

### Проблема: "База данных не подключается"

```bash
# Проверьте, запущен ли PostgreSQL
sudo systemctl status postgresql

# Запустите, если не запущен
sudo systemctl start postgresql

# Проверьте подключение
sudo -u postgres psql -d islamplus -c "\dt"
```

---

## ✅ Чеклист после установки

- [ ] Код загружен в GitHub
- [ ] Подключение к VPS работает
- [ ] Бот установлен через `install.sh`
- [ ] `.env` файл настроен с `BOT_TOKEN` и `ADMIN_IDS`
- [ ] База данных инициализирована
- [ ] Бот запущен и работает (`systemctl status`)
- [ ] Бот отвечает на `/start` в Telegram
- [ ] Тестовые команды работают
- [ ] Автозапуск включен (`systemctl is-enabled`)
- [ ] Обновление через `deploy.sh` работает

---

**Готово! 🎉 Теперь у вас есть полностью настроенный бот с автоматическим обновлением!**

Если возникнут вопросы - обращайтесь! 😊