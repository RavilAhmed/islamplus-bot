# 🚀 Быстрый старт для VPS

Минимальная инструкция для тех, кто хочет быстро установить бота на сервер.

## Шаг 1: Подготовка на компьютере

1. **Загрузите код в Git репозиторий** (GitHub, GitLab и т.д.)
2. **Запомните URL репозитория**

## Шаг 2: Установка на VPS

### 2.1 Подключитесь к серверу

```bash
ssh user@your-vps-ip
```

### 2.2 Клонируйте репозиторий

```bash
sudo git clone <URL_ВАШЕГО_РЕПОЗИТОРИЯ> /opt/islamplus-bot
cd /opt/islamplus-bot
```

### 2.3 Запустите установку

```bash
sudo chmod +x install.sh
sudo ./install.sh
```

Во время установки:
- Введите URL репозитория (если не указан через переменную)
- Ответьте на вопросы о PostgreSQL

### 2.4 Настройте .env файл

```bash
sudo nano /opt/islamplus-bot/.env
```

**Обязательно укажите:**
- `BOT_TOKEN` - получите у @BotFather в Telegram
- `ADMIN_IDS` - ваш Telegram ID (можно узнать у @userinfobot)

### 2.5 Перезапустите бота

```bash
sudo systemctl restart islamplus-bot
```

### 2.6 Проверьте статус

```bash
sudo systemctl status islamplus-bot
```

Если все хорошо, вы увидите `active (running)` ✅

## Шаг 3: Проверка

Откройте Telegram и отправьте боту команду `/start`

Должен прийти ответ с приветствием! 🎉

---

## 🔄 Обновление бота

Теперь когда вы изменили код на компьютере и запушили в Git:

**На сервере:**
```bash
cd /opt/islamplus-bot
sudo ./deploy.sh
```

Или еще проще:
```bash
cd /opt/islamplus-bot
sudo -u www-data git pull
sudo systemctl restart islamplus-bot
```

**Готово!** Бот обновлен за 3 команды! ✨

---

## 📝 Типичный workflow

1. **На компьютере:** Редактируете код
2. **На компьютере:** 
   ```bash
   git add .
   git commit -m "Описание изменений"
   git push
   ```
3. **На сервере:**
   ```bash
   cd /opt/islamplus-bot
   sudo ./deploy.sh
   ```

Вот и все! 🎊