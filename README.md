# IslamPlus.Practice - Telegram Bot

Бот для системного развития мусульманина через получение знаний, формирование привычек и практику.

## 🚀 Возможности

- 📚 **Курсы** - структурированные курсы с уроками
- 🛠 **Практика** - развитие навыков и привычек
- 🎯 **Ежедневный фокус** - концентрация на приоритетных навыках
- 🧠 **Большой тест** - проверка знаний по исламу
- 📈 **Прогресс** - отслеживание результатов и достижений
- 💎 **Система очков** - мотивация через геймификацию

## 📋 Требования

- Python 3.10+
- PostgreSQL 15+
- Redis 7+ (опционально)
- Docker и Docker Compose (для быстрого запуска)

## 🛠 Установка

### Для VPS (Production)

📖 **Подробная инструкция:** [DEPLOY.md](DEPLOY.md)

**Быстрый старт:**
```bash
git clone <URL_ВАШЕГО_РЕПОЗИТОРИЯ> /opt/islamplus-bot
cd /opt/islamplus-bot
sudo ./install.sh
```

**Обновление бота:**
```bash
cd /opt/islamplus-bot
sudo ./deploy.sh
```

### Для локальной разработки

#### 1. Клонирование репозитория

```bash
git clone <repository_url>
cd "Бот Ислам и Практика"
```

#### 2. Установка зависимостей

```bash
pip install -r requirements.txt
```

#### 3. Настройка окружения

Скопируйте `.env.example` в `.env` и заполните необходимые переменные:

```bash
cp .env.example .env
```

Отредактируйте `.env`:

```env
BOT_TOKEN=your_telegram_bot_token
DATABASE_URL=postgresql+asyncpg://islamplus:islamplus_password@localhost:5432/islamplus
REDIS_URL=redis://localhost:6379/0

ADMIN_IDS=123456789,987654321
```

#### 4. Запуск базы данных

Используйте Docker Compose для быстрого запуска:

```bash
docker-compose up -d
```

Или настройте PostgreSQL вручную:

```sql
CREATE DATABASE islamplus;
CREATE USER islamplus WITH PASSWORD 'islamplus_password';
GRANT ALL PRIVILEGES ON DATABASE islamplus TO islamplus;
```

#### 5. Инициализация базы данных

Запустите миграции Alembic:

```bash
alembic upgrade head
```

Или создайте таблицы напрямую (для разработки):

```bash
python init_db.py
```

#### 6. Запуск бота

```bash
python run.py
```

## 📁 Структура проекта

```
islamplus-bot/
├── src/
│   ├── bot.py              # Точка входа
│   ├── config.py           # Конфигурация
│   ├── database/           # Модели и миграции
│   │   ├── base.py
│   │   └── models.py
│   ├── handlers/           # Обработчики сообщений
│   │   ├── start.py
│   │   ├── courses.py
│   │   ├── practice.py
│   │   ├── quiz.py
│   │   ├── progress.py
│   │   └── settings.py
│   ├── services/           # Бизнес-логика
│   │   ├── user_service.py
│   │   ├── course_service.py
│   │   ├── skill_service.py
│   │   ├── quiz_service.py
│   │   └── notification_service.py
│   ├── keyboards/          # Клавиатуры
│   │   ├── main_menu.py
│   │   ├── courses.py
│   │   ├── practice.py
│   │   └── quiz.py
│   └── utils/              # Утилиты
├── alembic/                # Миграции БД
├── docker-compose.yml      # Docker конфигурация
├── requirements.txt        # Зависимости
└── README.md              # Документация
```

## 🔧 Команды бота

- `/start` - Регистрация и приветствие
- `/menu` - Главное меню
- `/progress` - Мой прогресс
- `/test` - Большой тест
- `/practice` - Раздел практики

## 🗄 База данных

Проект использует PostgreSQL с SQLAlchemy (async). Миграции выполняются через Alembic.

### Создание новой миграции:

```bash
alembic revision --autogenerate -m "Описание изменений"
alembic upgrade head
```

## 🔐 Безопасность

- Никогда не коммитьте `.env` файл
- Используйте сильные пароли для БД
- Ограничьте доступ к административным командам
- Регулярно обновляйте зависимости

## 📝 Лицензия

Проект разработан для образовательных целей.

## 🤝 Вклад

Приветствуются предложения и улучшения!

## 📞 Поддержка

При возникновении проблем создайте issue в репозитории.

---

**Примечание:** Это MVP версия. Многие функции находятся в разработке и будут добавлены в следующих версиях.